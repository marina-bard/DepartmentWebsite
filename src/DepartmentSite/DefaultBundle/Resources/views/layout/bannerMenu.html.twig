<ul class="links-list pull-right">
    {% for menuItem in menu | sort%}
        <li><a ng-href='/{{ _locale }}/{{ menuItem.getSlug() }}/show'>{{ menuItem.translate(_locale).getTitle() }}</a></li>
    {% endfor %}
</ul>
