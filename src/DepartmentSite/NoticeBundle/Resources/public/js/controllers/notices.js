'use strict';

var adverts_length;

angular.module('bsuir-ecm').controller('NoticesCtrl', function($scope) {
    $scope.notices = JSON.parse(data.replace(/"/g, '\'\'').replace(/&quot;/g, '"'));
});


