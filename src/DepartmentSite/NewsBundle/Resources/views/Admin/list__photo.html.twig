{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field%}
    <div >
        {% if object | itm_file_exists('photo') %}
            <img src="{{ object | itm_ipw_path('photo') | imagine_filter('preview') }}" />
        {% endif %}
    </div>
{% endblock %}