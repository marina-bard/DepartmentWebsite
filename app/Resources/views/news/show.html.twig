{% extends 'base.html.twig' %}

{% block body %}
    {# <h1>News</h1> <table> <tbody> <tr> <th>Id</th> <td>{{ news.id }}</td> </tr> <tr> <th>Title</th> <td>{{ news.title }}</td> </tr> <tr> <th>Description</th> <td>{{ news.description }}</td> </tr> <tr> <th>Content</th> <td>{{ news.content }}</td> </tr>
    <tr> <th>Photo</th> <td>{% if news.photo %}{{ news.photo|join(', ') }}{% endif %}</td> </tr> </tbody> </table> <ul> <li> <a href="{{ path('news_index') }}">Back to the list</a> </li> <li> {{ form_start(delete_form) }} <input type="submit"
    value="Delete"> {{ form_end(delete_form) }} </li> </ul> #}

    <div ng-controller="OneNewsCtrl">
        <script type="text/javascript">
            data = '{{ render(controller('DepartmentSiteNewsBundle:News:getOne', {'id' : news.id})) }}';
        </script>

        <div class="container">
            <div class="row">
                <div class="col-xs-12">
                    <div class="section-title">
                        <span>Новости</span>
                        <div class="v-separator"></div>
                        <a ng-href="{{ path('news_index') }}">К списку новостей</a>
                        <div class="v-separator"></div>
                        <a ng-href="#/news/page">К следующей</a>
                        <div class="v-separator"></div>
                        <a ng-href="#/news/page">К предыдущей</a>
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-xs-12">
                    <i class="news-time">{{'{{ news.created_at | date }}'}}</i>
                    <div class="post-title-inside-post">{{'{{ news.title }}'}}</div>
                    {#<br /><img alt="Cinque Terre" class="full-post-img" ng-src="{{news.photo.path}}" />#}
                    <div class="post-content-inside post-content" ng-bind-html="news.content | toHTML"></div>
                    <div class="share-block">
                        <div class="share">
                            <div class="sprite share-icon"></div>
                            <i>Поделиться новостью в соцсетях</i>
                            <div class="sprite facebook"></div>
                            <div class="sprite twitter"></div>
                            <div class="sprite vk"></div>
                            <div class="sprite instagram"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
